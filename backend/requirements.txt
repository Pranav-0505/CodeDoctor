# Code Doctor — Backend Dependencies
# Install with: pip install -r requirements.txt
# Requires Python 3.12+ (tested on 3.12, 3.13, 3.14)

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6

# Database ORM
sqlalchemy>=2.0.35

# Data Validation — pydantic>=2.10 ships prebuilt cp314 wheels (no Rust/MSVC needed)
pydantic>=2.10.0
pydantic-settings>=2.5.2

# Authentication / JWT
PyJWT>=2.9.0

# Testing
pytest>=8.3.3
pytest-asyncio>=0.24.0
httpx>=0.27.2

# Optional: PostgreSQL support (SQLite used by default)
# psycopg2-binary>=2.9.9

pathspec>=0.12.1
